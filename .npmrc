# pnpm 配置文件

# 使用严格的对等依赖检查
strict-peer-dependencies=false

# 启用提升（hoisting）以提高性能
shamefully-hoist=true

# 自动安装对等依赖
auto-install-peers=true

# 使用硬链接节省磁盘空间
node-linker=hoisted

# 设置 store 目录（可选）
# store-dir=~/.pnpm-store
