# ==========================================
# 🗄️ 数据库配置 (必须)
# ==========================================
# Vercel Postgres 或其他 PostgreSQL 数据库连接字符串
#
# 本地开发：
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/seo_monorepo"
#
# Vercel 生产环境（使用 Vercel Postgres）：
# DATABASE_URL="$POSTGRES_PRISMA_URL"  # 映射到 Vercel 自动生成的变量
#
# 或直接使用完整连接字符串：
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ==========================================
# 🔐 NextAuth 配置 (必须)
# ==========================================
# JWT 签名密钥（必须至少 32 字节）
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here-change-this"

# 应用完整 URL（必须包含协议，不要末尾斜杠）
# 本地开发: http://localhost:3100
# 生产环境: https://admin.yourdomain.com
NEXTAUTH_URL="https://admin.yourdomain.com"

# ==========================================
# 🔑 系统设置加密密钥 (推荐)
# ==========================================
# 用于加密敏感设置（如 API Keys）
# 生成方法: openssl rand -base64 32
# 如果不设置，将使用默认密钥（不安全）
SETTINGS_ENCRYPTION_KEY="your-encryption-key-here-change-this"

# ==========================================
# 🤖 OpenAI API (可选 - 可在后台设置页面配置)
# ==========================================
# OpenAI API Key（用于 AI SEO 优化功能）
# 优先级：数据库设置 > 环境变量
# 可以在管理后台 /settings 页面配置，无需在此设置
OPENAI_API_KEY="sk-your-openai-api-key"

# OpenAI 模型选择
# 可选: gpt-4-turbo, gpt-4, gpt-3.5-turbo
OPENAI_MODEL="gpt-4-turbo"

# ==========================================
# 🌐 网站配置
# ==========================================
# 管理后台
NEXT_PUBLIC_SITE_NAME="SEO 管理后台"

# 网站 1
NEXT_PUBLIC_WEBSITE1_URL="https://website1.yourdomain.com"
NEXT_PUBLIC_WEBSITE1_NAME="Telegram 网站 1"

# 网站 2
NEXT_PUBLIC_WEBSITE2_URL="https://website2.yourdomain.com"
NEXT_PUBLIC_WEBSITE2_NAME="Telegram 网站 2"

# 网站 TG
NEXT_PUBLIC_WEBSITE_TG_URL="https://www.telegramtgm.com"
NEXT_PUBLIC_WEBSITE_TG_NAME="Telegram TGM"

# ==========================================
# 📊 Google Analytics (可选)
# ==========================================
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# ==========================================
# 🔍 搜索引擎验证码 (可选)
# ==========================================
NEXT_PUBLIC_GOOGLE_VERIFICATION="your-google-verification-code"
NEXT_PUBLIC_BING_VERIFICATION="your-bing-verification-code"
NEXT_PUBLIC_YANDEX_VERIFICATION="your-yandex-verification-code"

# ==========================================
# 🗄️ Vercel Postgres (自动生成 - 无需手动配置)
# ==========================================
# 当你在 Vercel 创建 Postgres 数据库并连接到项目后
# 以下变量会自动添加，无需手动设置：
#
# POSTGRES_URL="postgres://default:xxx@xxx-pooler.us-east-1.postgres.vercel-storage.com/verceldb"
# POSTGRES_PRISMA_URL="postgres://default:xxx@xxx-pooler.us-east-1.postgres.vercel-storage.com/verceldb?pgbouncer=true"
# POSTGRES_URL_NO_SSL="postgres://default:xxx@xxx-pooler.us-east-1.postgres.vercel-storage.com/verceldb"
# POSTGRES_URL_NON_POOLING="postgres://default:xxx@xxx.us-east-1.postgres.vercel-storage.com/verceldb"
# POSTGRES_USER="default"
# POSTGRES_HOST="xxx-pooler.us-east-1.postgres.vercel-storage.com"
# POSTGRES_PASSWORD="xxx"
# POSTGRES_DATABASE="verceldb"

# ==========================================
# 📝 使用说明
# ==========================================
#
# 1. 本地开发：
#    - 复制此文件为 .env.local
#    - 填写本地数据库连接
#    - 设置 NEXTAUTH_URL=http://localhost:3100
#
# 2. Vercel 部署：
#    - 在 Vercel Dashboard → Settings → Environment Variables 中配置
#    - DATABASE_URL 使用 $POSTGRES_PRISMA_URL（映射）
#    - NEXTAUTH_URL 使用实际部署域名
#
# 3. 密钥生成：
#    openssl rand -base64 32
#
# 4. 环境变量优先级：
#    - API Keys：数据库设置 > 环境变量
#    - 其他配置：环境变量 > 默认值
#
# 5. 重要提示：
#    - 所有环境变量都在 turbo.json 的 globalEnv 中声明
#    - 添加新变量时，记得同时更新 turbo.json
#
# 详细文档：
# - DEPLOYMENT.md - 部署指南
# - VERCEL-POSTGRES-SETUP.md - 数据库配置
# - NEXTAUTH-SETUP.md - 认证配置
# - TURBOREPO-ENV-VARS.md - 环境变量配置
